@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Manage your tasks and users.</p>
    <a href="@Url.Action("Index", "Task")" class="btn btn-primary">View Tasks</a>
    <a href="@Url.Action("Index", "User")" class="btn btn-secondary">View Users</a>
</div>
